(defun send-ping (route)
  t)

(defun send-find-node (route id)
  ())

(defun send-find-value (route id)
  ())

(defun send-store (route id value)
  t)

(defun lookup-node (id)
  "recusively find the closest node that matches id"
  )